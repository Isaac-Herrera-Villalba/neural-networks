# ==============================================================
# Proyecto: Linear Regression (Python 3.12+)
# Descripción:
#   Dependencias necesarias para ejecutar el sistema de regresión
#   lineal (múltiple o simple) con generación automática de reportes PDF.
# ==============================================================

# --- Núcleo numérico y manejo de datos ---
numpy >= 1.26.4
pandas >= 2.2.2
python-dateutil >= 2.9.0.post0

# --- Lectura de hojas de cálculo ---
openpyxl >= 3.1.5     # Soporte para archivos .xlsx
odfpy >= 1.4.1        # Soporte para archivos .ods

# --- Generación de reportes y salida LaTeX ---
pylatexenc >= 2.10    # Codificación segura de símbolos LaTeX

# --- Mejoras opcionales de salida en consola ---
tabulate >= 0.9.0

# ==============================================================
# Notas:
# - Todos los paquetes son compatibles con Python 3.12 (Debian 13+)
# - No se requieren librerías de machine learning externas (scikit-learn, etc.)
# - Para compilar los reportes, debe tener instalado LaTeX (pdflatex o xelatex).
# ==============================================================

