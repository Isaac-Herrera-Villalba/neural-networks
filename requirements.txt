# ==============================================================
# Proyecto: Neural Networks (Python 3.12+)
# Descripción:
#   Dependencias necesarias para ejecutar los métodos de:
#     - Perceptrón
#     - Regla Delta (ADALINE)
#     - Backpropagation (MLP)
#
#   El sistema genera reportes matemáticos detallados en PDF
#   siguiendo exactamente las fórmulas de la práctica.
# ==============================================================

# --- Núcleo numérico y manejo de datos ---
numpy >= 1.26.4
pandas >= 2.2.2
python-dateutil >= 2.9.0.post0

# --- Lectura de hojas de cálculo ---
openpyxl >= 3.1.5     # Soporte para archivos .xlsx
odfpy >= 1.4.1        # Soporte para archivos .ods

# --- Generación de reportes y salida LaTeX ---
pylatexenc >= 2.10    # Codificación segura de símbolos LaTeX para ecuaciones

# --- Mejoras opcionales de salida en consola ---
tabulate >= 0.9.0

# ==============================================================
# Notas:
# - Todos los paquetes son compatibles con Python 3.12 (Debian 13+).
# - Este proyecto NO utiliza frameworks de ML externos
#   (no usa scikit-learn, TensorFlow o PyTorch).
# - La generación de reportes requiere un entorno LaTeX funcional:
#       pdflatex (TeX Live) o xelatex.
# - El entrenamiento es completamente determinista y reproducible.
# ==============================================================

